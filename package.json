{
  "name": "root",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npx lerna run build",
    "test": "npx lerna run test",
    "publish": "npx lerna publish --no-git-tag-version --no-push",
    "clean": "rimraf node_modules && npx lerna run clean"
  },
  "dependencies": {},
  "devDependencies": {
    "lerna": "^8.1.2"
  }
}
